# Product Roadmap — HanziQuest (`cc_review`)

_Last updated: 2026-08-23 · Authority hierarchy: see `AI_CONTRACT.md`_

**Current state:** Tier 1, Phase 2 complete. Pre-pilot.
Bundled phrase quiz mode shipped on 2026-05-02. Phrase-keyed input (item D) fully shipped 2026-08-13, including live QA of quiz-taking/packaging/grading — see §4.
Item I Phase 1 (article import → known/unknown triage) shipped 2026-08-17 — migration deployed, `verify:rls` passing for `paragraphs`, and live-QA'd in-browser; one real bug found and fixed along the way (pre-existing `TagCascadePicker.tsx` gap, unrelated to this feature's own code). Item I Phase 2 (paragraph library, re-import, test-mode prep) shipped 2026-08-18, same day. **Item I Phase 3 (paragraph quiz runtime — packaging + the actual playable quiz UI) fully shipped 2026-08-20** — migration deployed, `verify:rls` passing (59/60, the one failure is the pre-existing, documented, unrelated `vocab_phrase_lesson_tags` gap — see `0_BUILD_CONVENTIONS.md`); full live-QA checklist against the dev Supabase project passed, including a real bug found and fixed along the way (a stale-refetch race in `TestModeSection.tsx` silently dropped a just-added blank from packaging — see `docs/fix-log/build-fix-log-2026-08-20-test-mode-package-drops-blank-race.md`) — see item I row for detail. Two small paragraph-quiz extensions shipped 2026-08-22: an ingredient reward panel on quiz completion, and a reveal-after-3-bounces hint on the word bank — see §4.
No Tier 1 gates remain open (former mobile-results-layout gate dropped, see below).
Tier 2 is unblocked; Tier 3 remains blocked on Tier 2 completion.

---

## 1 · Active Work

> If a feature isn't in this table or §3, it's deferred — see §2.

| ID | Feature | Notes | Spec | Status |
|----|---------|-------|------|--------|
| C | Parent difficulty setting | Beginner-level AI prompts scoped per family; admin configures via Prompts page | TBD | 📋 Planned |
| D | Phrase-keyed input | Parent manages phrases (new `vocab_phrases` entity: Chinese phrase, pinyin, English definition, multiple example sentences) on the same Content Admin page as characters (Characters/Phrases toggle); batch-add via comma-separated list + batch tag assignment on `/words/add`; AI generation via a new `prompt_type`; taggable via a new `vocab_phrase_lesson_tags` join; packageable into fill-test sessions (own selection/packaging flow); a phrase blank uses the app's existing drag-and-match mechanic in its own round, never mixed with character rounds; correct answers nudge SRS familiarity of the phrase's own component characters (if already added), wrong answers touch nothing; no auto-SRS for the phrase itself; a correctly-answered phrase earns a flat 1 coin. **Unblocked item I.** | `docs/feature-specs/2026-07-26-phrase-keyed-input.md` | ✅ Shipped 2026-08-13 — quiz-taking, packaging, and `/words/add` batch entry all live-verified; see §4 for the bugs live QA found and fixed |
| F | Ingredient shopping for kids | Buy-only slice shipped 2026-08-23: the ingredient detail sub-modal on `/words/shop` shows per-unit coin cost and a quantity stepper (default 1, +/− or type directly), and a Buy button spends `cost × quantity` coins via the new `purchase_shop_ingredient` RPC (new `shop_ingredient_purchases` table + `shop_coin_transactions.ingredient_key`/`'purchase_ingredient'` action type, migration `20260823010000_shop_ingredient_purchases.sql`). Requires the target recipe to already be unlocked. Purchased ingredients also feed Shop Kitchen's cupboard availability (`buildShopIngredientAvailabilityMap`) alongside quiz rewards. Deliberately narrower than the original draft spec: no dedicated inventory page, no recipe-ready badge — those, plus purchasing special (non-base) ingredients, remain open follow-ups. | `docs/feature-specs/2026-03-30-shop-ingredient-shopping.md` | ✅ Shipped 2026-08-23 — migration deployed and confirmed via `db:status`; `scripts/verify-rls.ts` Section 11 added and passing 7/7 live against the dev Supabase project; `tsc`/`eslint`/`npm test`/`check:encoding` all pass. **Not yet done:** live-QA in an actual browser session (this pass verified RLS and automated tests only, not a real buy flow end-to-end in the UI). |
| G | Coin cash-out (redemption) | Kids draw real-dollar value from usable coin balance at 100 coins = $1; each redemption is documented with a child-supplied note and signature; usable balance decreases but quiz results (coins earned from sessions) are unaffected; lives on `/words/shop` | `docs/feature-specs/2026-05-11-coin-redemption.md` | ✅ Shipped 2026-05-11 |
| H | Save & resume test session progress | Child can pause any test session (due-review or packaged) and resume later; autosave after each answer, multiple paused sessions allowed, parent read-only visibility; new `review_session_progress` table, authorized 2026-07-24 | `docs/feature-specs/2026-07-24-save-resume-test-session-progress.md` | ✅ Shipped 2026-07-25 |
| J | Shop Kitchen | Child-facing cooking page at `/words/shop/kitchen` ("Shop Kitchen" in nav, right after "Recipe Shop"), child + platform-admin only. A cupboard shows every ingredient rewarded from paragraph quizzes (item 2026-08-22); a recipe book lists every recipe a platform admin has flagged with a `cook_method` (new nullable `shop_recipes` column) that the child has also already unlocked via the existing `/words/shop` coin-unlock flow; a stovetop/oven cooks a ready recipe by spending its required ingredients (new `cook_shop_recipe` RPC, atomic, `security definer` — a deliberate departure from this codebase's usual `security invoker` shop RPCs, since its two tables carry no RLS write policy at all; caught and corrected during implementation, see the feature spec's Scope section) and produces one row in a new `shop_cooked_dishes` table, landing on a default/unsorted shelf; three more color-coded shelves (Drinks, Desserts, Hot Meals) let the child drag a dish to organize it (new `move_shop_cooked_dish` RPC — the only field on any shop-adjacent table a child can mutate post-insert). Ingredient spend is tracked via a new append-only `shop_ingredient_consumptions` ledger, netted against `shop_ingredient_rewards` client-side (no running-balance column). Drag-and-drop is implemented with the Pointer Events API (not native HTML5 DnD) so it works by touch on iPads/phones, reusing the same `elementFromPoint` hit-testing technique already established in `ParagraphSpanSelector.tsx`. No coin cost to cook (ingredients only); no cap on re-cooking; no parent-facing visibility. Purchased ingredients (item F) are not yet a Cupboard source — item F is still unbuilt. | `docs/feature-specs/2026-08-23-kitchen-page.md` | 🔧 Code complete, migration deployed — authorized and built 2026-08-23: migration (`20260823000000_shop_kitchen.sql`), service/domain/UI layers, Shop Admin `cook_method` field, and unit tests (domain helpers, service-layer mocked-RPC tests, permissions, strings parity) all written; `tsc`/`eslint`/`npm test`/`check:encoding` all pass locally. **Updated 2026-08-23 (during item F work):** `supabase db push` confirmed applied to dev via `db:status`, and `scripts/verify-rls.ts` Section 10 (`shop_cooked_dishes`/`shop_ingredient_consumptions`) was added and run for the first time — it surfaced a real RLS gap, not yet fixed: `shop_ingredient_consumptions` has a direct-insert policy it shouldn't (contradicts Shop Kitchen Rule 3's "no direct client write policy at all"); see `0_BUILD_CONVENTIONS.md`'s verify-rls known-state note. **Still not done:** the full live-QA checklist against a dev Supabase project (a real cook, a real ingredient shortfall, and a real cross-shelf drag on an actual touch device). Do not mark this row shipped until the RLS gap is fixed and live-QA completes. |
| I | Article import → known/unknown triage | **Most immediate Tier 1 need.** Phase 1 shipped a new route, `/words/add-paragraph` ("Manage Paragraphs" in nav), parent/platform-admin only: paste a block of Chinese text, see it split into sentences with every Hanzi character and every substring matching an existing family phrase flagged known vs. unknown, click/drag-select spans (`ParagraphSpanSelector.tsx`), and bulk-add + tag the selection through the unmodified `/words/add` ingestion services, persisting to a new `paragraphs` table. Phase 2 added, on the *same* route (no new route): a filterable library list (title + tags), **Continue Import** (re-triage an existing paragraph against current family state to add more spans, additive-only, title editable, raw text immutable), and **Prep Fill Test** (a three-state known/ineligible/eligible span selector — reusing `ParagraphSpanSelector.tsx`'s token-building but click-to-toggle only — that carves eligible spans into a numbered word-bank block, previewing the eventual child-facing blank layout, and saves the selection as a named, editable **test mode** in a new `paragraph_test_modes` table, uniquely named *per paragraph* rather than family-wide). Phase 3 made a test mode actually playable: **Package as Quiz** (`TestModeSection.tsx`) snapshots a test mode's blanks into one `review_test_sessions` row (new `paragraph_test_mode_id` session-level discriminator); a genuinely new child-facing quiz UI (`ParagraphQuizReviewSection.tsx`, mounted at the existing `/words/review/fill-test` entry point, no new route) renders the whole paragraph as continuous text paginated into ~20-blank pages, each with its own shuffled word bank, drag-or-click-to-place, wrong drops bounce back with no penalty, grade tier derived from retry count (1st=easy/2nd=good/3rd+=hard, no `again`), session-level coin formula judged by error rate; autosave/resume reuses the existing `review_session_progress` machinery. Phase 3 also generalized three already-shipped surfaces for consistency: `/words/all`'s Delete actions now block on any active packaged session's target (character, phrase, mixed, or paragraph-quiz alike); Delete Test Mode and the new Delete Paragraph action both block-then-confirm on an active session; `/words/results` replaced its inline truncated character lists with a clickable popup, for every session row. See `0_ARCHITECTURE.md`'s "Add Paragraph Rules" (rules 1–21) and "Fill-Test Review Rules" (28+) sections for full behavior. | `docs/feature-specs/2026-08-17-add-paragraph-article-import.md` (Phase 1), `docs/feature-specs/2026-08-17-paragraph-fill-test.md` (Phase 2), `docs/feature-specs/2026-08-19-paragraph-quiz-runtime.md` (Phase 3) | ✅ Phase 1 shipped 2026-08-17; ✅ Phase 2 shipped 2026-08-18 — migrations deployed and confirmed via `db:status`; `scripts/verify-rls.ts` Sections 7–8 pass live for `paragraphs`/`paragraph_test_modes`, including the per-paragraph (not family-wide) unique-name constraint; manually QA'd in-browser end to end (import → filter → Continue Import → Prep Fill Test → create/edit/duplicate-reject a test mode). Live QA surfaced two real bugs, both fixed same day: (1, Phase 1) `TagCascadePicker.tsx`'s custom grade/unit/lesson value rendered blank after confirming — pre-existing gap in that shared component, fixed by porting `AddSection.tsx`'s existing `appendSelectedOption` fix over; (2, Phase 2) `vm.words`/`vm.vocabPhrases` went stale after a successful add and were never refreshed, so a second submission later in the same session (e.g. Continue Import right after the original import) re-triaged against outdated data and tried to re-insert something already added, failing with a real Postgres unique-constraint conflict instead of the intended silent skip — fixed by calling `refreshAllData()` after every successful word/phrase insert in both `AddParagraphSection.tsx` and `ContinueImportSection.tsx`. See `docs/fix-log/build-fix-log-2026-08-17-tag-cascade-picker-custom-value-blank.md` and `docs/fix-log/build-fix-log-2026-08-18-paragraph-stale-triage-state.md`. **✅ Phase 3 shipped 2026-08-20** — migration (`20260819000001_paragraph_quiz_sessions.sql`) deployed and confirmed via `db:status`; `scripts/verify-rls.ts` 59/60 (the single failure is the pre-existing, documented, unrelated `vocab_phrase_lesson_tags` gap, not a regression from this change); full automated test suite (762 tests), `tsc --noEmit`, `eslint`, and `npm run check:encoding` all pass. **Full live-QA checklist against the dev Supabase project passed** — packaging a session, playing it through as a child across multiple pages, autosave/resume mid-page, grading/coins/results, and the delete-blocking behaviors all verified. Live QA surfaced one real bug, found and fixed same day: **Package as Quiz could silently drop a just-added blank** — `TestModeSection.tsx`'s test-mode-list effect was keyed on the whole `paragraph` object (a new reference on every `vm.paragraphs` update, including the one a test-mode edit's own span-materialization step triggers), racing that same edit's own, correct `setTestModes` call; when the effect's stale refetch resolved second it silently overwrote the just-saved test mode with the pre-edit version, so packaging read a stale in-memory list even though the database was correct the whole time — fixed by keying the effect on the paragraph's id instead of the whole object. See `docs/fix-log/build-fix-log-2026-08-20-test-mode-package-drops-blank-race.md`. |

> Planned features with a `TBD` spec require a feature spec before build. See `AI_CONTRACT.md §2` for scope confirmation rules.

---

## 2 · Deferred — Do Not Build

Stop and confirm before acting on any of these:

- Tier 2 / Tier 3 features of any kind
- Content pack import, purchase, or monetization
- New AI provider integrations
- Flashcard data export / import

---

## 3 · Tier Strategy

| Tier | Name | Scope | Status |
|------|------|-------|--------|
| 1 | Controlled Micro Context | Word-level Spaced Repetition System (SRS), controlled cognitive load | 🔄 Active |
| 2 | Structured Text Context | Phrase/paragraph fill tasks | 🔄 Active — item I shipped under an explicit gate exception authorized 2026-08-17 (see item I); former blocking Tier 1 gate has since been dropped |
| 3 | Authentic Reading Layer | Long-form reading comprehension | 🔒 Not started — blocked on Tier 2 |

---

## 4 · Shipped Features (reference only)

All prior feature specs are archived at `docs/archive/specs/`.
Note that specs may not reflect the current implementation — the codebase has evolved through multiple iterations and specs were not always updated to match. Treat them as historical context, not ground truth. When in doubt, read the code.

- 2026-04-22: Quiz Results can package one session's failed Hanzi into a named review test session via the existing packaged-session flow.
  Spec: `docs/feature-specs/2026-04-22-results-failed-to-test-session.md`
- 2026-05-02: Bundled phrase quiz mode allows one- and two-phrase characters to enter fill-test sessions through runtime bundles, with per-character grading and unchanged coin logic.
  Spec: `docs/feature-specs/2026-05-02-grouped-phrase-quiz-mode.md`
- 2026-05-11: Coin cash-out (redemption) ships on `/words/shop`. Kids redeem usable coins at 100:1 for real-dollar value with a child-written note and signature. Four-part breakdown panel replaces single wallet balance display. `redeem_coins` RPC is atomic; quiz results remain immutable.
  Spec: `docs/feature-specs/2026-05-11-coin-redemption.md`
- 2026-07-23: Parents can delete a single packaged character from an active review test session on Due Review without deleting the whole session; deleting the last remaining character deletes the session. No new RLS, RPC, or schema — reuses the existing parent-scoped delete policy on `review_test_session_targets`.
  Spec: `docs/feature-specs/2026-07-23-delete-target-from-review-test-session.md`
- 2026-07-25: Save & resume test session progress — children can pause any fill-test session (ad-hoc due-review or packaged) and resume later from Due Review's unified "Paused Sessions" list. Autosave fires after every graded word; resume re-validates the unanswered tail against current content (and, for packaged sessions, current packaged targets) and never re-grades an already-graded word. New `review_session_progress` table with family-scoped read (parent read-only visibility) / user-scoped write RLS; packaged-session cleanup happens server-side in `complete_review_test_session`.
  Spec: `docs/feature-specs/2026-07-24-save-resume-test-session-progress.md`
- 2026-08-13: Phrase-keyed input (item D) completed live QA against a live Supabase dev project, closing the spec's "Handoff — Current State" punch list. An actual quiz run surfaced four real bugs that no amount of mocked testing had caught: Due Review's quiz-ready count/gate ignored phrase targets entirely, so a phrase-only packaged session always read 0/0 and could never start; starting a packaged session always routed through the character-only flashcard phase first, which bounced a phrase-only session straight back out; `getSessionMetadata()` resolved `family_id`/`user_id` from the Supabase JS client's `session.user.app_metadata` (the `auth.users` DB row, frozen since the 2026-08-08 session-scoped-claims migration stopped writing it) instead of the access token's own claims, silently failing autosave RLS for any profile other than whichever happened to be active before that migration; and `cloneFillTest` silently dropped `vocabPhraseMembers` when building the live quiz queue, so phrase-round grading, `vocab_phrases.test_count`, and the familiarity nudge had never actually fired in production despite passing every mocked unit test. Also shipped: a flat 1-coin rule for a correctly-answered phrase (distinct from the character `easy/good/hard/again` coin table), and an inline correct-answer reveal — ruby pinyin plus, for phrase rounds, a bilingual definition card — shown under each wrong sentence in the fill-test review step, for both character and phrase rounds.
  Spec: `docs/feature-specs/2026-07-26-phrase-keyed-input.md`
- 2026-08-17: Item I Phase 1 — Article import → known/unknown triage ships a new `/words/add-paragraph` route ("Manage Paragraphs" in nav, parent/platform-admin only): paste a block of Chinese text, see it split into sentences with every Hanzi character and every substring matching an existing family phrase flagged known vs. unknown, click/drag-select spans, and bulk-add + tag the selection through the unmodified `/words/add` ingestion services, persisting to a new `paragraphs` table. Live QA (in-browser, end to end) found and fixed one real bug, a pre-existing gap unrelated to this feature's own code: `TagCascadePicker.tsx`'s custom grade/unit/lesson value rendered blank after confirming.
  Spec: `docs/feature-specs/2026-08-17-add-paragraph-article-import.md`
- 2026-08-18: Item I Phase 2 — adds, on the same route, a filterable paragraph library (title + tags), Continue Import (re-triage an existing paragraph against current family state, additive-only), and Prep Fill Test (a three-state known/ineligible/eligible span selector that carves eligible spans into a numbered word-bank preview and saves the selection as a named, per-paragraph test mode in a new `paragraph_test_modes` table). Live QA found and fixed one real bug: stale `vm.words`/`vm.vocabPhrases` after a successful add caused a second submission later in the same session to re-triage against outdated data and attempt a duplicate insert instead of silently skipping.
  Spec: `docs/feature-specs/2026-08-17-paragraph-fill-test.md`
- 2026-08-20: Item I Phase 3 — Paragraph Quiz Runtime makes a test mode actually playable: Package as Quiz snapshots a test mode's blanks into one `review_test_sessions` row, and a new child-facing quiz UI (mounted at the existing `/words/review/fill-test` entry point, no new route) renders the whole paragraph as continuous text paginated into ~20-blank pages with per-page shuffled word banks, drag-or-click-to-place, no-penalty bounce-back on wrong drops, grade tier derived from retry count, and session-level coins by error rate; autosave/resume reuses the existing `review_session_progress` machinery. Also generalized Delete-blocking (on any active packaged session's target) to paragraph-quiz targets and replaced `/words/results`' truncated character lists with a clickable popup. Full live-QA checklist passed; live QA found and fixed one real bug, a stale-refetch race in `TestModeSection.tsx` that could silently drop a just-added blank from packaging.
  Spec: `docs/feature-specs/2026-08-19-paragraph-quiz-runtime.md`
- 2026-08-22: Paragraph Quiz Ingredient Reward — finishing a paragraph-quiz session now rewards up to 3 random, distinct ingredients pooled across every recipe the child has already unlocked, shown in an inline reward panel (real `public/ingredients/*.png` artwork) before returning to Due Review. New append-only `shop_ingredient_rewards` table plus a `reward_random_ingredients` RPC (`security invoker`, matching `unlock_shop_recipe`/`redeem_coins`) that verifies caller and session ownership, is idempotent per `quiz_session_id`, and samples server-side so the client never influences the outcome; a reward-RPC failure never blocks session completion. `verify-rls.ts` gained a new Section 9 (7/7 passing); RPC-level smoke tests confirmed real-recipe pooling, idempotency, ownership, and the empty-pool case. Live QA (in-browser reward panel walkthrough) completed 2026-08-23 — fully shipped.
  Spec: `docs/feature-specs/2026-08-22-paragraph-quiz-ingredient-reward.md`
- 2026-08-22: Paragraph Quiz Reveal-After-3-Bounces — a word-bank item that's been dragged onto a wrong blank 3 times, cumulative across whichever blanks it was tried on, turns purple and gains a "Hint" (提示) badge opening a read-only popup (flashcard content for a character blank; phrase/pinyin/meaning/first example for a phrase blank); opening it never touches grading, coins, or `retryCount`. Tracked via a new `wrongDragCounts` progress-data field (no schema migration — `progress_data` is jsonb) independent of the existing `retryCount` that still drives grading. Live QA found and fixed two placement bugs same-day: the affordance belongs on the bank item, not the blank pill in the paragraph text; and eligibility must count wrong drags of the specific dragged item, not wrong attempts at whichever blank absorbed them. Manual UI confirmation (popup content rendering, and verifying the Hint click fires no grading/selection side effects) completed 2026-08-23 — fully shipped.
  Spec: `docs/feature-specs/2026-08-22-paragraph-quiz-reveal-after-bounces.md`
